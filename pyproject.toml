[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "opendart-mcp-server"
version = "1.4.2"
description = "OpenDART MCP server with 85 disclosure APIs and verified financial statement Excel workbooks"
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
authors = [
    { name = "gyeongmin100", email = "hopekmb1196@gmail.com" },
]
keywords = ["mcp", "opendart", "dart", "korea", "stock", "disclosure", "llm", "claude"]
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
]
dependencies = [
    "mcp[cli]>=1.0.0,<2",
    "httpx>=0.27.0",
    "xmltodict>=0.13.0",
    "beautifulsoup4>=4.12.0",
    "lxml>=5.0.0",
    "openpyxl>=3.1.0",
]

[project.urls]
Homepage = "https://github.com/gyeongmin100/Open-Dart-Plugin"
Repository = "https://github.com/gyeongmin100/Open-Dart-Plugin"

[project.scripts]
opendartmcp = "opendartmcp.server:main"

[tool.hatch.build.targets.wheel]
packages = ["src/opendartmcp"]
